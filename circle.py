import math


def area(r):
    """принимает радиус(r)
    возвращает площадь круга (π * r²)"""
    return math.pi * r * r


def perimeter(r):
    """принимает радиус(r)
    возвращает длину окружности (2 * π * r)"""
    return 2 * math.pi * r

