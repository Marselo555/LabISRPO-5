def area(a, h):
    """получает длину(a), высоту(h)
    возвращет площаль (a * h / 2)"""
    return a * h / 2

def perimeter(a, b, c):
    """получает a, b, c
    возвращает площaдь (a + b + c)"""
    return a + b + c
